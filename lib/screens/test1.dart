import 'package:flutter/material.dart';

class test1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vũ Quang Huy"),
        backgroundColor: Colors.black,
      ),
      body: Center(child: Text("LOl")),
    );
  }
}
